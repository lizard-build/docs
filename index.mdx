---
description: "Deploy an app, run a worker, connect Managed Postgres, or run code in Sandboxes. Choose a task, follow a guide, and check the limits."
---

# Lizard documentation

Lizard builds and runs applications from a GitHub repository or a local folder. Use Lizard CLI or the dashboard to deploy a web service, run a background worker, connect Managed Postgres or Managed Redis, and inspect logs. Use Lizard SDK to create Sandboxes for code execution.

## Choose a task

| I want to… | Start here |
|---|---|
| Deploy my first app | [App quickstart](/getting-started) |
| Deploy Next.js, React, Vue, or a Python app | [Framework guides](/framework-guides) |
| Deploy from Claude Code or another coding agent | [Coding agent guide](/guides/deploy-from-coding-agent) |
| Host a remote MCP server | [Remote MCP guide](/guides/deploy-mcp-server) |
| Keep a Telegram bot running | [Telegram worker guide](/guides/telegram-bot) |
| Connect a database | [Managed Postgres](/addons/postgres) |
| Run generated code in a separate environment | [Sandboxes quickstart](/sandboxes/quickstart) |
| Keep files after a sandbox ends | [Persistent Volumes](/sandboxes/volumes) |

## Deploy an app

Install Lizard CLI and sign in, then deploy a repository you can access:

```bash
npm install -g @lizard-build/cli
lizard login
lizard add -r your-org/your-app
```

Lizard builds the image on its servers, so this path does not need local Docker. [lizardpack](/concepts/build-pipeline) detects supported projects; projects with special build needs can use explicit commands or a Dockerfile. Check the build logs and verify the application URL after deployment.

## Understand your resources

A **workspace** groups members and projects. A **project** groups services, databases, and Sandboxes. A **service** runs an application or worker. Managed Postgres, Managed Redis, and Managed Object Storage provide data services that applications connect to through environment references.

Apps and Sandboxes have different runtime and storage rules. Read [limits](/platform/limits) and [storage and recovery](/platform/storage-and-recovery) before depending on a resource's size, lifetime, or data retention.

## Find the right level of detail

- [Guides](/guides) cover a task from setup to a check of the result.
- [Framework guides](/framework-guides) give build commands, adapters, ports, and checks for each stack.
- [Core concepts](/concepts/architecture) explain projects, builds, and deployments.
- [Lizard CLI reference](/cli) lists commands, flags, and exit codes.
- [Troubleshooting](/deploy/troubleshooting/service-never-healthy) helps diagnose a failed start.
- [Known issues](/platform/known-issues) records limits and recovery cases to check before a release.
